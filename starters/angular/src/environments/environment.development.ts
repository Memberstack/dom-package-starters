// Used by `ng serve`. `ng build` uses environment.ts instead.
//
// Leave the Test Mode key here. When you put your Live key in environment.ts,
// this file keeps local development on test members rather than creating real
// ones while you work.
export const environment = {
  memberstackPublicKey: "__MEMBERSTACK_PUBLIC_KEY__",
};
